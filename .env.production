# Production build config. Vite loads .env.production for `npm run build`
# (production mode) with HIGHER priority than .env.local, so the dev-only
# localhost override in .env.local never leaks into a production bundle.
VITE_API_BASE_URL=https://repair.handatransportation.com/api/v1
